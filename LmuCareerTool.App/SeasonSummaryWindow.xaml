<Window x:Class="LmuCareerTool.App.SeasonSummaryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Sesong fullført" Width="680" MinWidth="600"
        SizeToContent="Height" MaxHeight="920"
        Background="{StaticResource BgColor}"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResizeWithGrip">
    <DockPanel Margin="24" LastChildFill="True">

        <ScrollViewer DockPanel.Dock="Top" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <StackPanel>
                <TextBlock x:Name="TitleText" Text="🏆 Sesong fullført!" FontSize="24" FontFamily="Segoe UI Semibold" Margin="0,0,0,4" />
                <TextBlock x:Name="SubTitleText" Text="-" Foreground="{StaticResource MutedTextColor}" Margin="0,0,0,16" TextWrapping="Wrap" />

                <TextBlock x:Name="ContractStatusText" Text="" Foreground="{StaticResource WarnColor}" Margin="0,0,0,16"
                           TextWrapping="Wrap" Visibility="Collapsed" />

                <Border x:Name="ResultsBorder" Style="{StaticResource CardStyle}" Padding="12" Margin="0,0,0,16" MaxHeight="180">
                    <DataGrid x:Name="ResultsGrid" AutoGenerateColumns="False" IsReadOnly="True">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Runde" Binding="{Binding Round}" Width="55" />
                            <DataGridTextColumn Header="Bane" Binding="{Binding Track}" Width="*" />
                            <DataGridTextColumn Header="Resultat" Binding="{Binding Result}" Width="90" />
                            <DataGridTextColumn Header="Poeng" Binding="{Binding Points}" Width="60" />
                        </DataGrid.Columns>
                    </DataGrid>
                </Border>

                <Border Style="{StaticResource CardStyle}" Padding="16" Margin="0,0,0,12">
                    <StackPanel>
                        <TextBlock Text="1. VELG KLASSE" FontFamily="Segoe UI Semibold" FontSize="14" Margin="0,0,0,10" />
                        <ComboBox x:Name="ClassComboBox" Padding="8,6" SelectionChanged="ClassComboBox_SelectionChanged" />
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource CardStyle}" Padding="16" Margin="0,0,0,4">
                    <StackPanel>
                        <TextBlock Text="2. OVERGANGSVINDU - velg og signer et tilbud for å starte sesongen" FontFamily="Segoe UI Semibold" FontSize="14" Margin="0,0,0,10" TextWrapping="Wrap" />
                        <TextBlock x:Name="NoOffersText" Text="Ingen tilbud tilgjengelig for denne klassen akkurat nå." Foreground="{StaticResource MutedTextColor}" Visibility="Collapsed" Margin="0,0,0,8" />
                        <ItemsControl x:Name="OfferList">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="{StaticResource SurfaceColor}" CornerRadius="8" Padding="14" Margin="0,0,0,10">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>
                                            <StackPanel Grid.Column="0">
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock Text="{Binding Title}" FontFamily="Segoe UI Semibold" FontSize="15" VerticalAlignment="Center" />
                                                    <Border Background="{StaticResource AccentColor}" CornerRadius="4" Padding="6,2" Margin="10,0,0,0" VerticalAlignment="Center">
                                                        <TextBlock Text="{Binding BadgeText}" FontSize="10" Foreground="White" FontFamily="Segoe UI Semibold" />
                                                    </Border>
                                                </StackPanel>
                                                <TextBlock Text="{Binding Subtitle}" Foreground="{StaticResource MutedTextColor}" FontSize="12" Margin="0,2,0,0" />
                                                <TextBlock Text="{Binding ReasoningText}" TextWrapping="Wrap" Margin="0,8,0,0" />
                                                <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                                                    <TextBlock Text="{Binding LengthText}" Foreground="{StaticResource MutedTextColor}" FontSize="12" Margin="0,0,14,0" />
                                                    <TextBlock Text="{Binding SalaryText}" Foreground="{StaticResource GoodColor}" FontSize="12" />
                                                </StackPanel>
                                                <TextBlock Text="{Binding GoalText}" Foreground="{StaticResource WarnColor}" FontSize="12" Margin="0,4,0,0" TextWrapping="Wrap" />
                                            </StackPanel>
                                            <Button Grid.Column="1" Content="{Binding SignButtonText}" VerticalAlignment="Center" Padding="14,10" Margin="14,0,0,0"
                                                    IsEnabled="{Binding CanAfford}" Click="SignOfferButton_Click" Tag="{Binding}" />
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </DockPanel>
</Window>
